import Phaser from "phaser";

export class battle extends Phaser{
    constructor(shareData){
        super(scene)
        this.shareData = shareData
        this.scene = scene 
    }
    update(){
        
    }

}